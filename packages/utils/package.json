{
  "name": "@rxova/utils",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Private monorepo tooling: the release gate (verify), doc-snippet and Playwright browser-list checks, and the tarball smoke test. Scripts run from the repo root (see root package.json).",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "test:coverage": "vitest run"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "vitest": "^4.1.11"
  }
}
